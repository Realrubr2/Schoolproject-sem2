{
  "scripts": {
    "test": "set TS_NODE_COMPILER_OPTIONS={\"module\": \"commonjs\"} && mocha -r ts-node/register 'test/*.test.ts'"
  },
  "devDependencies": {
    "mocha": "^9.1.3",
    "sinon": "^11.1.1",
    "ts-node": "^10.4.0"
  }
}
